# Changelog — Quran Revision App

> Backlog en attente + décisions utiles pour les prochains devs qui reprendront ces items. L'historique détaillé des sprints livrés (features, bugs corrigés, refactors) vit dans `git log`, pas ici. Pour *comment* le code fonctionne aujourd'hui (architecture, moteurs métier, écrans), voir `docs/DOCUMENTATION_TECHNIQUE.md`.

---

## Décisions à connaître avant de reprendre un item du backlog

- **`ayah_facts` est la seule source de vérité du plan quotidien** — aucun recalcul à la volée dans les écrans : un seul moteur (`AppState.ensureDayPlan()`) écrit les lignes du jour dans cette table, tout le reste (nombre de versets du jour, sourates à réviser, streak, fraîcheur) se **lit** depuis la table plutôt que d'être recalculé indépendamment ailleurs. Toute feature touchant plan/récap/check-in doit respecter ce principe — c'est justement la dette que la Phase 6 a corrigée (voir `docs/DOCUMENTATION_TECHNIQUE.md`).
- **Migration SQLite sans backfill (2026-08-30)** — au dernier changement de schéma (introduction d'`ayah_facts`), les anciennes tables ont été droppées sans backfill ligne-par-ligne, décision prise parce qu'aucun utilisateur réel n'existait encore en production. **Ne pas reproduire cette décision sans redemander confirmation** si un futur changement de schéma intervient alors que l'app a de vrais utilisateurs.
- **Profils élèves multi-utilisateurs retirés délibérément (2026-09-01)** — `StudentProfile`/`StudentService`/`StudentProfileBar` ont existé puis ont été supprimés en entier suite à un retour utilisateur explicite (app mono-utilisateur, fonctionnalité jamais utile en pratique) — pas un oubli ni un nettoyage silencieux. Ne pas les réintroduire sans un besoin confirmé.
- **`RevisionUnit`/`SourateSelection` ont deux notions d'identité distinctes** — `RevisionUnit.==` (égalité de valeur sur `sourate.id`+`verseStart`+`verseEnd`) coexiste avec un dédoublonnage par `label` (String d'affichage) dans `plan_screen.dart`, et avec une clé composite `String` manuelle dans `AppState._sameSelections`. Aucune divergence de comportement connue aujourd'hui, mais à réconcilier (probablement vers `==`/`hashCode` sur les deux modèles) si l'un de ces fichiers est retouché — voir item dette technique ci-dessous.
- **`ayah_facts` = journal, pas juste une table de stats (2026-09-01)** — retour TestFlight sur "sourate démarrée sans verset appris disparaît" : la cause réelle n'était pas un manque de persistance ponctuel mais une rupture de la sémantique `reach=0` (visé/proposé) → `reach=1` (atteint) déjà en place côté `revise`, jamais appliquée côté `learn`. Corrigé en généralisant cette sémantique (`startLearning` écrit `ayah_id=1, reach=0` ; `unlearnVerse` repasse `reach=0` au lieu de `DELETE`) plutôt qu'en ajoutant un correctif local. Règles détaillées dans `CLAUDE.md` § « Modèle de données central : `ayah_facts` » — à relire avant toute nouvelle feature touchant apprentissage/révision/fraîcheur, ce fichier est le point de connexion entre tous ces domaines.
- **Cases cochées de PlanScreen fusionnées dans `ayah_facts.reach` (2026-09-02)** — `AppState._checkedRakaas` (état en mémoire + `SharedPreferences`, Sprint 7) supprimé : `PlanScreen` écrit désormais directement `reach` pour la plage exacte de la rakaa cochée (`AppState.toggleTodayUnitReach`/`reachStatusFor`, une seule requête groupée en Dart plutôt qu'une requête par rakaa). Conséquence assumée : si la pénurie de matière fait apparaître la même plage de versets sur 2 rakaas (`RevisionEngine._padCyclically`), cocher l'une coche l'autre automatiquement — `reach` est une vérité par verset/jour, pas par rakaa.
- **`sqflite_common_ffi` en dépendance de prod, pas dev-only (2026-09-02)** — `main.dart` bascule `databaseFactory` vers l'implémentation FFI desktop derrière `Platform.isWindows` (jamais vrai sur iOS/Android), ajouté pour pouvoir tester l'app sur Windows en local (aucun device mobile disponible sur cette machine). Contrepartie assumée : Dart ne fait pas de tree-shaking sur une branche `if (Platform.isX)` (condition runtime, pas compile-time), donc le paquet est bel et bien compilé dans le binaire iOS/Android même s'il n'y est jamais exécuté — léger surcoût de taille de binaire mobile pour un besoin purement dev. Un vrai découplage demanderait des points d'entrée séparés (`main_desktop.dart` compilé via `--target`) — pas fait, jugé disproportionné pour l'usage actuel (tests ponctuels sur cette machine, pas un besoin produit).
- **Vue Coran unifiée derrière `ayah_start`/`ayah_end` (2026-09-02)** — même principe que « `ayah_facts` seule source de vérité » ci-dessus, appliqué à l'affichage du texte coranique : `VerseBottomSheet`/`VerseRow` (`lib/widgets/`) sont le seul mécanisme d'affichage d'une plage de versets (sourate + `ayahStart` + `ayahEnd`), utilisé par Plan du jour, Récap et Apprendre. Toute nouvelle feature qui affiche du texte coranique pour une plage de versets doit réutiliser ce couple plutôt que réimplémenter une boucle `VerseService.getVerse` — c'est exactement la dette que `SurahReaderScreen` (supprimé) et le chiffre arabe-indic redondant avec un badge (supprimé) représentaient. Voir `docs/DOCUMENTATION_TECHNIQUE.md` §8.6.
- **Deux `daysRemaining` distincts et volontairement non unifiés (2026-09-02)** — `AppState.cycleSummary` (affichage HomeScreen/RecapScreen) peut tomber à 0 ("objectif atteint") ; `RevisionEngine.selectDayUnits`/`DaySelection.daysRemaining` (moteur, via `AppState.dayUnits`/`buildTodaySession`) reste toujours ≥ 1 pour ne jamais diviser par zéro dans `dailyTarget`. Une revue de code a suggéré de les unifier (faire dériver `cycleSummary` de `DaySelection` telle quelle) — délibérément non fait : ça aurait supprimé le message "objectif atteint" en cas de cycle dépassé. Ne pas fusionner ces deux valeurs sans retrancher explicitement ce comportement d'abord.
- **`reach` par défaut au check-out ≠ `reach` par défaut à la proposition (décision de cadrage 2026-09-04, implémentée Sprint 7)** — le défaut "fait" retenu pour l'écran check-out ne s'applique **qu'à l'affichage/la confirmation de `CheckOutScreen`**, pas à `AyahFactsService.proposeUnits` ni au flux `PlanScreen`/`toggleTodayUnitReach`, qui doivent garder `reach=0` tant que l'utilisateur n'a pas coché une rakaa en priant. Si ce défaut venait à être déplacé vers `proposeUnits`, `PlanScreen` afficherait tout comme déjà fait avant même d'avoir prié — ne pas confondre les deux couches en retouchant cette zone. `CheckOutScreen` garde son "décoché" purement local (`Set<RevisionUnit>`, aucune écriture par tap) jusqu'à la clôture, où `AppState.markUnitsReached(units, {date, reach})` écrit le batch final dans les deux sens.
- **Décocher au check-out doit explicitement réécrire `reach=0`, jamais un no-op (bug trouvé en revue de code, Sprint 7)** — une unité peut déjà avoir `reach=1` en base au moment où `CheckOutScreen` s'ouvre (rakaa cochée plus tôt dans `PlanScreen`, le jour n'ayant jamais été scellé). Ne confirmer en base que les unités *restées* cochées (et ignorer silencieusement celles décochées) laisse leur ancien `reach=1` en place — l'utilisateur voit sa correction acceptée à l'écran mais `AppState.checkOut()` compte quand même l'unité comme faite. Toute future écriture "décocher/annuler" sur `ayah_facts` doit écrire explicitement la valeur inverse plutôt que simplement s'abstenir d'écrire — voir CLAUDE.md § « Modèle de données central » ("Annuler une progression repasse `reach` à 0, ne supprime jamais la ligne").
- **Récap : le retour TestFlight "chiffres pas bons" (2026-09-01) était déjà résolu au moment du scoping suivant (2026-09-04)** — la divergence concrète entre écrans (`HomeScreen`/`RecapScreen` recalculant chacun indépendamment) a été corrigée par le sprint "simplify-ayah-facts" (`AppState.cycleSummary`, 2026-09-02) avant même que ce retour soit retraité en scoping. Vérification faite sur le code actuel : `cycleSummary.pos` est déjà ancré dans `ayah_facts` (`AppState.checkOut` ne l'avance que sur des unités réellement `reach=1`) ; les autres chiffres de `RecapScreen` (`selections.length`, `totalVerses`, `cycleTotal`) décrivent le **plan configuré** (portée), pas l'activité — légitime, pas un bug. Item retiré du Backlog ; à rouvrir seulement si un exemple concret et récent de chiffre incorrect est signalé.
- **Fraîcheur par verset (Phase 8 Sprint 1, 2026-09-04) — comportement visible qui change délibérément** : le seuil "récente" passe de 7j (ancien badge `hot`) à 30j partout, et remplace le seuil 180j de l'ancienne section "À prioriser" (`S.needsAttention`) par les niveaux `neverRevised`/`sixMonths`/`oneYear` (`freshnessNeedsAttention`, `lib/widgets/freshness_badge.dart`) — une sourate revue il y a 10-20j n'affiche plus de badge "à surveiller", et une sourate partiellement récente (`partiallyRecent`) n'entre plus dans "À prioriser" alors qu'elle pouvait dépasser l'ancien seuil de 180j sur son verset le plus ancien. Choix non explicitement tranché au scoping (« sans forcément être identiques, contextes UI différents ») — à ajuster si retour utilisateur en ce sens plutôt qu'à considérer comme un bug.
- **`/code-review` incomplet sur le sprint fraîcheur-par-verset (Phase 8 Sprint 1, 2026-09-04)** — 3 des 10 agents lancés (scan ligne-à-ligne du diff, audit des comportements retirés, pièges spécifiques Dart) ont échoué avec une erreur de limite de session ("You've hit your session limit"), pas de bug applicatif. Les 7 autres angles (reuse, simplification, efficacité, altitude, conventions CLAUDE.md, wrapper/proxy, traçage cross-fichier) ont conclu normalement et remonté des findings réels, tous appliqués (voir `docs/DOCUMENTATION_TECHNIQUE.md`, note du 2026-09-04). Compensé par une relecture manuelle ciblée (références orphelines, null-safety, force-unwrap, migration SQL) plutôt que par une relance des agents en échec — 59 tests passent, `flutter analyze` propre. **Si un bug est découvert plus tard dans ce sprint et qu'aucune des notes ci-dessus/de la doc technique ne l'explique, relire ce diff en particulier** (les angles non couverts étaient justement ceux les plus susceptibles d'attraper un bug ligne-à-ligne classique) plutôt que de supposer une couverture `/code-review` complète comme pour un sprint normal.

---

## Backlog

**Axe "data model"** — les items P1/P2 en tête de table découlent directement de la correction du bug "sourate qui disparaît" (analyse 2026-09-01) : `ayah_facts` (journal daté, `reach=0` visé → `reach=1` atteint, granularité verset) est déjà capable d'alimenter bien plus que ce qu'il alimente aujourd'hui — voir `CLAUDE.md` § « Modèle de données central ». Avant de les lancer, trancher lesquels entrent dans le prochain sprint plutôt que de les traiter à la volée. **Fraîcheur par verset livrée le 2026-09-04 (Phase 8 Sprint 1)** — voir décision ci-dessus ; item retiré du backlog.

| Priorité | Feature | Notes |
|----------|---------|-------|
| P1 | **Réduire la duplication + unifier le hero `check_in_screen.dart`/`check_out_screen.dart` (scopé 2026-09-05)** | Périmètre exact retenu (choix utilisateur, y compris les 2 arbitrages de design ci-dessous) : (1) extraire un widget partagé (`lib/widgets/`) pour le hero du haut (`Container` `palette.primary`, `borderRadius` bas 20, eyebrow uppercase 10sp/w700/letterSpacing 2.5, titre 21-22sp/w600) — **arbitrage tranché : le style "badge à bordure" de check-out devient le seul style**, check-in abandonne son sous-titre italique sans bordure au profit du même badge (avec son propre texte, `S.checkInVersesProposed`) — c'est le seul changement visuel de ce sprint ; (2) extraire un widget partagé pour le `TextSpan` "Sourate · v.X–Y" utilisé à l'identique dans `_CheckOutRow` (check_out_screen.dart:374-391) et `_UnitRow` (check_in_screen.dart:272-289) ; (3) fusionner `_CheckOutDetailScreen` (check_out_screen.dart:419-498) et `_CheckInDetailScreen` (check_in_screen.dart:399-483) en un seul écran détail paramétrable (Scaffold+AppBar "Sourate · v.X–Y" + label uppercase + `Wrap` de `VerseChip`) — check-out garde son toggle "à retravailler" (bookmark), check-in garde son chip "+" d'extension + son texte d'astuce ; les deux comportements restent distincts via des paramètres/callbacks du widget fusionné, pas un seul comportement partagé. **Arbitrage tranché : la ligne de sourate (`_CheckOutRow` checkbox+lien "voir versets" vs `_UnitRow` avatar arabe+fraîcheur+icône fermer) reste telle quelle des deux côtés** — pas une vraie duplication, la checkbox de check-out bascule un état réel ("fait/pas fait") tandis que l'avatar de check-in est purement décoratif (rien à cocher au stade du check-in) ; seul le texte "Sourate · v.X-Y" à l'intérieur s'unifie (point 2). **Explicitement exclu** : la barre CTA du bas (`_ctaBar`, ~3-4 lignes identiques Padding+SizedBox+PrimaryCtaButton dans les deux fichiers) — trop trivial pour justifier une extraction (règle CLAUDE.md « Simplicity First »). **Vérification** : le point (1) change un pixel visible (hero de check-in) — vérifier avec `flutter run -d chrome` ou `-d windows` avant de clore le sprint (`flutter doctor` confirme les deux devices disponibles sur cette machine, contrairement à une ancienne note ici qui prétendait le contraire — le support Windows avait justement été ajouté le 2026-09-02 pour ce genre de test local, voir décision `sqflite_common_ffi` ci-dessus). Recoupe partiellement (volet CheckInScreen/CheckOutScreen uniquement) l'item "Reste de la dette technique §8.5" ci-dessous, qui garde ses autres volets (hadith/streak `learn_surah_screen.dart`, filtre de recherche sourate) non traités par ce sprint |
| P2 | **SRS réel par verset (score de difficulté depuis l'historique `ayah_facts`)** | `ayah_facts` garde potentiellement plusieurs lignes datées par verset (visé le J1, atteint le J5, re-flagué "à retravailler" le J20...) — un historique de rétention déjà présent, aujourd'hui écrasé par `lastRevisionDatesPerVerse` qui ne garde que `MAX(date)` par verset. Un score simple (ex. proportion de fois où le flag "à retravailler" — `needsWork`, ex-`cold` — a été posé / nombre de révisions) permettrait de prioriser automatiquement le plan du jour vers les versets réellement fragiles plutôt qu'un cycle round-robin uniforme — aucune nouvelle table nécessaire, seulement de nouvelles requêtes sur le journal existant. **Dépendance à reconfirmer** (relevé au scoping 2026-09-04) : ce score se base sur le flag "à retravailler", pas directement sur les paliers de fraîcheur temporelle (`FreshnessEngine`, livré 2026-09-04) — les deux partagent le même grain verset dans `ayah_facts` mais la dépendance annoncée n'est peut-être pas stricte ; à vérifier avant de lancer ce sprint plutôt que de la supposer |
| P2 | **Coran en SQLite (texte + word-by-word + tajweed)** | Un seul chantier d'infra débloquerait d'un coup 2 items déjà en backlog ("Jeu mot arabe → traduction", "Affichage tajweed coloré", tous deux bloqués faute de données QUL word-by-word/tajwid) et permettrait un JOIN naturel `ayah_facts` ↔ texte du verset — aujourd'hui deux mondes séparés (SQLite pour les faits, JSON en mémoire via `QuranTextAsset` pour le texte). **Garde-fou** : ne pas migrer "parce que c'est plus propre" — CLAUDE.md interdit la sur-ingénierie ; ce chantier ne se justifie que s'il sert une feature réellement engagée (le jeu mot-à-mot ou le tajweed), pas en soi |
| P3 | **Timeline d'activité (heatmap) dans Profil/Récap** | chaque fait `ayah_facts` est déjà daté — un calendrier d'activité façon GitHub (jours actifs, intensité = versets faits ce jour-là) est gratuit en donnée, juste une nouvelle requête + un nouveau widget. Complète le streak actuel (nombre de jours consécutifs) par une vue de régularité sur la durée |
| P3 | **Gamification narrative [H]** | vision long terme — direction artistique déjà validée (Mus'haf/Tahajjud), reste à définir la mécanique narrative |
| P3 | **Cycle wrap affiché** | si cyclePosition + totalUnits > cycleTotal, afficher "(cycle bouclé)" dans le summary bar |
| P2 | **Mode "versets à retravailler" en jeu à part** | écran dédié en dehors du plan du jour classique pour retravailler spécifiquement les versets signalés "à retravailler" (flag `needsWork`, ex-`cold`) et/ou peu récents (`FreshnessLevel`), en mode ludique — nécessite de définir la mécanique de jeu avant d'implémenter (valeur vs complexité à évaluer) |
| P3 | **Jeu mot arabe → traduction (Apprendre)** | mini-jeu : verset découpé mot par mot, l'utilisateur retrouve la bonne traduction parmi plusieurs propositions — travaille la compréhension du sens en plus de la mémorisation par cœur. Nécessite les données word-by-word (ressource QUL "QPC Hafs script - Word by word") — voir "Coran en SQLite" ci-dessus |
| P3 | **Affichage tajweed coloré** | coloration du texte par règle de tajwid (ressource QUL "QPC Hafs Tajweed") — attention à passer par `AppPalette`/tokens sémantiques pour rester cohérent clair/sombre, pas de couleurs de tajwid en dur — voir "Coran en SQLite" ci-dessus |
| P3 | **Animation badge de fraîcheur à la validation** | la mini barre de cycle donne un feedback de progression, mais le badge de fraîcheur (`FreshnessBadge`) de la sourate qui vient d'être validée ne s'anime pas (scale/color transition) pour rendre la connexion action → état plus explicite dans PlanScreen |
| P3 | **Support `sqflite` sur Flutter Web** | `AyahFactsService` (streak, historique, apprentissage) plante sur web faute de `sqflite_common_ffi_web` — limitation préexistante. Le web n'est de toute façon qu'une cible de "prévisualisation" (§2 doc technique), pas une cible livrée |
| P3 | **`dayFacts` peut afficher une plage trop large si le shuffle sépare une sourate découpée sur 2 jours** | le groupement par `MIN`/`MAX(ayah_id)` suppose les versets d'une sourate contigus pour le jour — vrai en pratique sauf si le shuffle (activé par défaut) place deux morceaux non-adjacents d'une même sourate découpée (>150 mots) sur deux jours différents. Cas rare, corrigerait en stockant l'identité de l'unité plutôt qu'en la reconstruisant par min/max |
| P3 | **Config modifiable pendant un jour en attente de check-out** | rien n'empêche d'éditer sourates/rythme (Profil) tant qu'un jour reste non scellé — `AppState.checkOut` recalculerait alors sur une config différente de celle utilisée à la génération du plan en attente. Corrigerait en bloquant l'édition de config (ou en avertissant) tant que `AppState.pendingDate != null` |
| P3 | **Rail de navigation alphabétique sur la liste des sourates (onboarding)** | les en-têtes Hizb épinglés (sticky headers) réduisent déjà la sensation de liste plate, mais un rail type Contacts iOS (lettre/index à droite pour sauter directement à une section) irait plus loin |
| P3 | **`PresetDropdown`/`VerseRangePicker` non migrés vers `AppPalette`** | ces deux widgets utilisent encore `Theme.of(context).colorScheme` au lieu de `context.palette` — dérogation pré-existante, à corriger si l'un de ces widgets est retouché |
| P3 | **Reste de la dette technique §8.5 de `docs/DOCUMENTATION_TECHNIQUE.md`** | arithmétique de cycle dupliquée dans `plan_screen.dart` (`_summaryBar` recalcule encore `cycleEnd` localement — `home_screen.dart`/`recap_screen.dart` résolus au sprint "simplify-ayah-facts" via `AppState.cycleSummary`), composants dupliqués (hadith/streak dans `learn_surah_screen.dart`, filtre de recherche sourate) — le volet `CheckInScreen`/`CheckOutScreen` est traité par le P1 ci-dessus, retiré de cette liste — candidats naturels pour un prochain sprint de simplification |
| P3 | **`unlearnVerse` ne filtre pas par date, contrairement à `setReach`** | `AyahFactsService.unlearnVerse(surahId, ayahId, riwaya)` fait `UPDATE ... WHERE surah_id=? AND ayah_id=? AND riwaya=? AND type='learn'` sans clause `date` — repasse `reach=0` sur *toutes* les lignes datées de ce verset, pas seulement celle du jour. Si un verset a été appris un jour J1 puis désappris un jour ultérieur (deux lignes distinctes par design, voir CLAUDE.md § « Modèle de données central »), la ligne J1 est aussi rétrogradée — contredit "deux dates distinctes coexistent, ce qui préserve l'historique". Trouvé en revue de code (sprint "simplify-ayah-facts", 2026-09-02), non corrigé car hors périmètre de ce sprint (ne touchait pas `ayah_facts_service.dart`) et improbable en pratique aujourd'hui (aucun consommateur n'exploite encore l'historique multi-dates côté apprentissage) |
| P3 | **`recentDayVerseStats` fait 2 requêtes SQL au lieu d'une** | `AyahFactsService.recentDayVerseStats` appelle `recentDayVerseCounts` (requête 1, `reach=1`) puis refait une requête quasi identique sans le filtre `reach` pour le total proposé (requête 2) — une seule requête avec agrégation conditionnelle (`SUM(CASE WHEN reach=1 THEN 1 ELSE 0 END)`, `COUNT(*)`) couvrirait les deux. Relevé 3 fois en revue de code (sprint "simplify-ayah-facts", 2026-09-02) sans être corrigé car hors périmètre (fichier non touché par ce sprint) — gain mesurable mais faible (RecapScreen, pas un chemin chaud) |
| P2 | **Récap : permettre de flaguer un verset "à retravailler" directement depuis le Récap** | aujourd'hui `AppState.setVerseNeedsWork` n'est appelable que depuis le détail du check-out (`_CheckOutDetailScreen`) — pas de main sur ce flag depuis `RecapScreen`/`VerseBottomSheet` alors que c'est l'endroit naturel où l'utilisateur relit une sourate en dehors du rituel quotidien |
| P3 | **Unifier la vue "sourates" entre check-in et récap** | le check-in (`CheckInScreen`, liste "chapelet" des unités du jour, éditable) et le Récap (`SouratesRecapCard`, toute la sélection, lecture seule avec badges de fraîcheur) montrent tous deux une liste de sourates mais avec des données et des permissions différentes — avant de fusionner l'affichage, trancher laquelle des deux fonctions (édition du jour vs vue d'ensemble) doit dominer |
| P3 | **Retour "Récap incomplet" (vague)** | retour TestFlight non actionnable tel quel — recoupe probablement les deux items ci-dessus ; à réévaluer une fois faits pour voir si le retour persiste |

---

## Convention de commit

```
feat(sprint-N): description
fix(sprint-N): corrections lié au commentaires utilisateurs
```
