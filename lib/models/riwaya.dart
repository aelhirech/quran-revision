/// Riwaya (transmission) du texte coranique affiché à l'utilisateur.
/// N'affecte que le texte arabe affiché — la numérotation des versets
/// (utilisée par RevisionEngine) est identique entre les deux éditions.
enum Riwaya { hafs, warsh }
