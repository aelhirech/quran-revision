class SessionRecord {
  final DateTime date;
  final int unitsCompleted;
  final int totalUnits;

  const SessionRecord({
    required this.date,
    required this.unitsCompleted,
    required this.totalUnits,
  });
}
